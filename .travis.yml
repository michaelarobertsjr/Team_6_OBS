matrix:
  include:
    - language: python
      python: 3.7
      script:
        - pip install -r requirements.txt
        - pylint main.py

    - language: node_js
      node_js: node
      script:
        - npm install

#before_install:
#  - nvm install node
#  - pyenv install 3.7.0a1

#install:
#  - npm install
#  - pip install -r requirements.txt
